#pragma once

// This global flag indicates whether the application is running under GW2AL.
// It can be used in shared code to slightly alter behavior if needed.
inline bool g_addon_loader_present = false;
