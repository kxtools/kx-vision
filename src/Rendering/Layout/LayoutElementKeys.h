#pragma once

namespace kx {

enum class LayoutElementKey {
    Distance,
    HealthBar,
    EnergyBar,
    PlayerName,
    GearSummary,
    DominantStats,
    Details,
    Count // Special member to get the number of keys
};

} // namespace kx
